# Pinned to the Django 4.1 dependency ceiling (see the deploy repo's
# docs/CVE-TRIAGE.md for why Django is pinned at 4.1.13 rather than upgraded).
Django==4.1.13

# 3.15.2 already requires Django>=4.2; 3.15.1 is the last release supporting Django>=3.0.
djangorestframework==3.15.1

# Ceiling forced by Django 4.1: django-extensions 4.x requires Django>=4.2.
django-extensions==3.2.3

# Abandoned upstream (last release 2021-06-27). django-q2>=1.11 requires
# Django>=5.2, so it is not available while pinned to 4.1. Documented as a
# known limitation.
django-q==1.3.9

# Django 4.1 does not support psycopg3 (added in Django 4.2) -- psycopg2 is
# the only option here. Compiled (not -binary) build in the Docker image.
psycopg2==2.9.10

# CVE-2026-28684 (symlink-following in set_key()/unset_key()) fixed in 1.2.2.
python-dotenv==1.2.3

gunicorn==23.0.0
whitenoise==6.12.0

# App dependencies (discovered by scanning imports across Exchange/ --
# upstream shipped no requirements.txt at all).
Pillow==12.3.0
plotly==5.24.1
pytz==2024.2
requests==2.32.3
bitcoin==1.1.42
eth-account==0.14.0
django-credit-cards==0.4.1
